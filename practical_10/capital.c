#include <stdio.h>
void capital(char s[], int len){
	for(int i=0; i<len; i++){
		
	}
}
int main(){
	char ss[];
	printf("For capital string : ");
	scanf("%[^\n]", ss);
	int len = sizeof(ss)/sizeof(ss[0]);
	
	capital(ss, len);

	return 0;
}
